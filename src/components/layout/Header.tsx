import { Header as HeaderMantine, useMantineColorScheme } from "@mantine/core"
import { DarkMode } from "../DarkMode"

export const Header = () => {

    return (
        <HeaderMantine height={60} p="xs">
            {/* <DarkMode /> */}
        </HeaderMantine>
    )
}

