import { Center } from "@mantine/core"
import { Layout } from "../../components/layout/Layout"

export const UserProfile = () => {
    return <div>
        <Center>
            Not connected
        </Center>
    </div>
}